apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: default
type: Opaque
data:
  ROOT_USER: "{{ minio_root_user | b64encode }}"
  ROOT_PASSWORD: "{{ minio_root_password | b64encode }}"